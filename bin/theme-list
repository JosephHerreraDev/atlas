#!/usr/bin/env bash

set -euo pipefail

ATLAS_PATH="${ATLAS_PATH:-$HOME/.local/share/atlas}"
THEMES_DIR="$ATLAS_PATH/themes"

[[ -d "$THEMES_DIR" ]] || exit 0

find "$THEMES_DIR" -mindepth 2 -maxdepth 2 -type f -name 'colors.toml' \
  -printf '%h\n' \
  | sed 's|.*/||' \
  | sort
